@model Coevery.Workflows.ViewModels.AdminEditViewModel
@using Coevery.Workflows.Models;
@using Coevery.Workflows.ViewModels;

@using (Html.BeginFormAntiForgeryPost()) {
    @Html.ValidationSummary()

    <fieldset>
        <div>
            <label for="name">@T("Name")</label>
            @Html.TextBox("name", String.Empty, new { @class= "text medium" })
            <span class="hint">@T("The name of the Workflow")</span>
        </div>
    </fieldset>
    
    <fieldset>
        <button class="primaryAction" type="submit">@T("Save")</button>
    </fieldset>
}