<header id="page-title">
    <h1 class="pull-left">@T("User Detail ") <span>show user detail information</span></h1>
</header>

<section id="main-content" style="margin-top: 47px">
    <div id="page-actions" class="clearfix">
        <btn-actions>
            <button type="button" class="btn btn-small" data-ng-click="exit()">Back</button>
            <button type="button" class="btn btn-small btn-primary" data-cobusy="{tracker:'saveUser'}" data-ng-click="saveAndView()">Save</button>
            <button class="btn btn-small btn-primary" data-cobusy="{tracker:'saveUser'}" data-ng-click="saveAndBack()">Save & Back</button>
        </btn-actions>
    </div>
    <div class="row-fluid">
        <div class="span12">
            <div class="form-horizontal no-border">
                @using (Html.BeginFormAntiForgeryPost(Url.Action("Create"),
                    FormMethod.Post,
                    new {@class = "form-horizontal edit-mode", @name = "myForm"})) {
                    @Display(Model)
                }
            </div>
        </div>
    </div>
</section>