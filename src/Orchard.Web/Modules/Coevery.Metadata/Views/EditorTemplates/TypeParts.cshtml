@model IEnumerable<Coevery.Metadata.ViewModels.EditTypePartViewModel>
@if (Model.Any()) {
    <fieldset>@foreach (var part in Model) {
            var p = part;
            @Html.EditorFor(m => p, "TypePart", p.Prefix)}
    </fieldset>}