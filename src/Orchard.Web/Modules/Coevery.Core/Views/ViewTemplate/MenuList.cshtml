<div id="nav-selector">
    <div class="dropdown">
        <a id="selector" class="dropdown-toggle" role="button" data-toggle="dropdown" href="#">
            <span data-label-placement title="{{navdata.curr.Text.Text}}">{{navdata.curr.Text.Text}}</span><b class="caret"></b>
        </a>
        <ul class="dropdown-menu bullet" role="menu" aria-labelledby="dLabel">
            <li data-ng-repeat="nav in navdata.alls" data-ng-class="{active: $uiRoute}" data-ui-route="{{nav.Href}}/?.*">
                <a href="{{nav.Href}}" title="{{nav.Text.Text}}">
                    <label><strong>{{nav.Text.Text}}</strong></label>
                </a>
            </li>
        </ul>
    </div>
</div>

<nav id="navigation">
    <ul class="collapse in">
        <li  data-ng-repeat="menu in navdata.curr.Items" data-ng-class="{active: $uiRoute}" data-ui-route="{{menu.Href}}/?.*">
            <a href="{{menu.Href}}" title="{{menu.Text.Text}}">
                <i class="{{menu.classesstr}} vertical-middle"></i>
                <span class="nav-title vertical-middle">{{menu.Text.Text}}</span>
            </a>
        </li>
    </ul>
</nav>
