<div class="row-fluid">
    <section class="" data-ag-grid="gridOptions" data-ag-grid-name="contentList"></section>
</div>

@foreach (var editor in Model.FilterEditors) {
    if(editor == null) {
         continue;
     }
    <script type="text/ng-template" id="@editor.Id">
    @Display(editor)
    </script>
}