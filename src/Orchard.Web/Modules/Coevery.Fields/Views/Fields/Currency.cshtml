@using Coevery.Fields.Settings;
@using Orchard.Utility.Extensions;
@{
    decimal? value = Model.ContentField.Value;
    if (value.HasValue) {
        var valueToDisplay = value.Value.ToString("C" + Model.DecimalPlaces);
        if (!string.IsNullOrWhiteSpace(valueToDisplay)) {
            string name = Model.ContentField.DisplayName;
            <span class="control-label">@T(name)</span>
            <div class="controls">
                <span class="control-text">@T(valueToDisplay)</span>
            </div>
        }
    }
}
